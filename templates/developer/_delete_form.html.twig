<form method="post" action="{{ path('app_developer_delete', {'id': developer.id}) }}" onsubmit="return confirm('Вы уверены ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ developer.id) }}">
    
    <button class="btn">Удалить</button>
</form>
